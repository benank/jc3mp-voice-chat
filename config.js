module.exports = 
{
    talk_key: 'Q', // Default push to talk key
    min_distance: 10, // Distance until volume starts decreasing
    max_distance: 50, // Maximum distance until volume is 0
    host: 'MY SERVER IP', // Your server's IP here
    port: 9000 // Port to use
}